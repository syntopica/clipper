// The client id of the `brain clipper` GitHub App (app id <app-id>, owned by
// @BusiRocket, installed on <owner>/<clips-repo> alone). Public by design: the
// device flow is a public-client flow and needs no client secret, so shipping
// this in the extension bundle leaks nothing.
export const GITHUB_APP_CLIENT_ID = '<client-id>'
